<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page.seoTitle or page.title }} | {{ settings.site_name }}</title>
  <meta name="description" content="{{ page.seoDesc or page.excerpt }}">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/themes/ecommerce/assets/css/theme.css">
</head>
<body class="{% block bodyClass %}{% endblock %}">
  <header class="site-header">
    <div class="container">
      <a href="/" class="site-logo">
        <span class="logo-icon">◆</span>
        {{ settings.site_name or 'Modern Shop' }}
      </a>
      <nav class="main-nav">
        <a href="/" class="{% if currentPath == '/' %}active{% endif %}">Home</a>
        <a href="/products" class="{% if currentPath | startswith('/products') %}active{% endif %}">Shop</a>
        <a href="/blog" class="{% if currentPath | startswith('/blog') %}active{% endif %}">Blog</a>
        {% if menus.main %}
          {% for item in menus.main %}
            {% if item.url not in ['/', '/products', '/blog'] %}
              <a href="{{ item.url }}" class="{% if currentPath == item.url %}active{% endif %}">{{ item.label }}</a>
            {% endif %}
          {% endfor %}
        {% endif %}
      </nav>
      <div class="header-actions">
        <button class="btn-icon search-toggle" aria-label="Search">
          <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="9" cy="9" r="7"/><line x1="14" y1="14" x2="19" y2="19"/>
          </svg>
        </button>
        <a href="/products" class="btn btn-primary">Shop Now</a>
      </div>
      <button class="mobile-menu-toggle" aria-label="Menu">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  <main>{% block content %}{% endblock %}</main>

  <footer class="site-footer">
    <div class="container">
      <div class="footer-grid">
        <div class="footer-brand">
          <a href="/" class="footer-logo">
            <span class="logo-icon">◆</span>
            {{ settings.site_name or 'Modern Shop' }}
          </a>
          <p>{{ settings.site_tagline or 'Premium Products for Modern Living' }}</p>
          <div class="social-links">
            <a href="#" aria-label="Twitter"><svg width="20" height="20" fill="currentColor"><path d="M23 3a10.9 10.9 0 01-3.14 1.53 4.48 4.48 0 00-7.86 3v1A10.66 10.66 0 013 4s-4 9 5 13a11.64 11.64 0 01-7 2c9 5 20 0 20-11.5a4.5 4.5 0 00-.08-.83A7.72 7.72 0 0023 3z"/></svg></a>
            <a href="#" aria-label="Instagram"><svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1.5"/></svg></a>
            <a href="#" aria-label="Facebook"><svg width="20" height="20" fill="currentColor"><path d="M18 2h-3a5 5 0 00-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 011-1h3z"/></svg></a>
          </div>
        </div>
        <div class="footer-links">
          <h4>Shop</h4>
          {% for cat in productCategories %}
            <a href="/products/category/{{ cat.slug }}">{{ cat.name }}</a>
          {% endfor %}
        </div>
        <div class="footer-links">
          <h4>Company</h4>
          {% if menus.footer %}
            {% for item in menus.footer %}
              <a href="{{ item.url }}">{{ item.label }}</a>
            {% endfor %}
          {% else %}
            <a href="/about">About Us</a>
            <a href="/contact">Contact</a>
            <a href="/faq">FAQ</a>
          {% endif %}
        </div>
        <div class="footer-newsletter">
          <h4>Newsletter</h4>
          <p>Get updates on new products and special offers</p>
          <form class="newsletter-form">
            <input type="email" placeholder="Enter your email">
            <button type="submit" class="btn btn-primary">Subscribe</button>
          </form>
        </div>
      </div>
      <div class="footer-bottom">
        <p>{{ settings.footer_copyright or '© ' + year + ' Modern Shop. All rights reserved.' }}</p>
        <div class="footer-legal">
          <a href="/privacy">Privacy</a>
          <a href="/terms">Terms</a>
        </div>
      </div>
    </div>
  </footer>

  <script src="/themes/ecommerce/assets/js/theme.js"></script>
</body>
</html>
